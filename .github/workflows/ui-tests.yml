name: SauceDemo UI Tests

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
  workflow_dispatch:

jobs:
  ui-tests:
    name: ${{ matrix.browser }} UI tests
    runs-on: ubuntu-latest

    strategy:
      fail-fast: false
      matrix:
        browser: [chrome, firefox]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven

      - name: Run tests on ${{ matrix.browser }}
        run: >
          mvn -B clean test
          -Psingle-browser
          -Dbrowser=${{ matrix.browser }}
          -Dheadless=true

      - name: Generate Allure report
        if: always()
        run: |
          mvn -B allure:report
          target/.allure/bin/allure awesome target/allure-results \
            --output target/allure-single-file-report \
            --single-file \
            --report-name "SauceDemo ${{ matrix.browser }} UI Tests"

      - name: Upload Allure report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: allure-report-${{ matrix.browser }}
          path: target/allure-single-file-report
          if-no-files-found: ignore

      - name: Upload failure screenshots
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-${{ matrix.browser }}
          path: target/screenshots
          if-no-files-found: ignore
